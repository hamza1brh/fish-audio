# Voice Service Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# Server Settings
# =============================================================================
VOICE_HOST=0.0.0.0
VOICE_PORT=8000
VOICE_WORKERS=1
VOICE_LOG_LEVEL=info

# =============================================================================
# TTS Provider Selection
# Options: s1_mini, elevenlabs, mock
# Default is mock (no model required)
# =============================================================================
VOICE_TTS_PROVIDER=mock

# =============================================================================
# S1 Mini Model Settings (required when VOICE_TTS_PROVIDER=s1_mini)
# =============================================================================
# REQUIRED: Path to the S1 Mini model checkpoint directory
# Download from: https://huggingface.co/fishaudio/openaudio-s1-mini
# VOICE_S1_CHECKPOINT_PATH=/path/to/openaudio-s1-mini

# Optional: Device (cuda, cpu, mps)
VOICE_S1_DEVICE=cuda

# Optional: Enable torch.compile for 10x faster inference (Linux/CUDA only)
VOICE_S1_COMPILE=false

# Optional: Use half precision (fp16)
VOICE_S1_HALF=false

# =============================================================================
# Reference Storage (for zero-shot cloning)
# Options: local, huggingface, s3
# =============================================================================
VOICE_REFERENCE_STORAGE=local

# Local storage path (relative to voice-service/)
VOICE_LOCAL_REFERENCE_PATH=references

# Hugging Face storage (when VOICE_REFERENCE_STORAGE=huggingface)
# VOICE_HF_REFERENCE_REPO=your-username/voice-references
# VOICE_HF_TOKEN=hf_xxxxx

# S3 storage (when VOICE_REFERENCE_STORAGE=s3)
# VOICE_S3_BUCKET=my-voice-bucket
# VOICE_S3_PREFIX=references/
# VOICE_AWS_REGION=us-east-1

# =============================================================================
# ElevenLabs (when VOICE_TTS_PROVIDER=elevenlabs)
# =============================================================================
# VOICE_ELEVENLABS_API_KEY=your-api-key
# VOICE_ELEVENLABS_MODEL=eleven_multilingual_v2

# =============================================================================
# LoRA Settings (optional)
# =============================================================================
VOICE_LORA_ENABLED=false
# VOICE_LORA_PATH=/path/to/lora/checkpoint.ckpt

# =============================================================================
# Inference Settings
# =============================================================================
VOICE_MAX_TEXT_LENGTH=2000
VOICE_DEFAULT_CHUNK_LENGTH=200
VOICE_STREAMING_ENABLED=true
